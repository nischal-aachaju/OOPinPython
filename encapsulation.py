# encapsulation -> wrapping data and functions into single units(object)

# when class is created then it is a capsule which has attributes and methods(function). So, this is encapsulation
class Student:
    def __init__(self,name):
        self.name=name


s1=Student("Nirjal")
